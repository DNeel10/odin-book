<!DOCTYPE html>
<html>
  <head>
    <title>OdinBook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="container">
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
        <div class="container-fluid">
          <% if user_signed_in? %>
            <%= link_to "Odin-Book", posts_path, class: 'navbar-brand' %>

            <span class="navbar-brand">
              Signed in as: 
                <% if current_user.profile && current_user.profile.name %>
                  <%= current_user.profile.name %> |
                <% else %>
                  <%= current_user.email %> |
                <% end %>
              <%= link_to "Profile", user_profile_path(current_user), class: "link-light", style: 'text-decoration: none' %> | 
              <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "navbar-link link-light", style: 'text-decoration: none' %>
            </span>
          <% else %>
            <%= link_to "Odin-Book", pages_home_path, class: 'navbar-link link-light', style: 'text-decoration: none' %>
            <span>
              <%= link_to "Log In", new_user_session_path, class: "navbar-link link-light", style: 'text-decoration: none' %> | 
              <%= link_to "Sign Up", new_user_registration_path, class: "navbar-link link-light", style: 'text-decoration: none' %>
            </span>
          <% end %>
        </div>
      </nav>
      
      <%= yield %>
    </div>
  </body>
</html>
